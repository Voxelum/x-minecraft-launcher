export const STORE_READY = 'store-ready';

/**
 * Client send it to server when it requires reload the config.
 */
export const RELOAD = 'reload';

/**
 * Client send it to server when it requires close its window.
 */
export const CLOSE = 'close';

/**
 * Client send to server to close all windows and shutdown server.
 */
export const EXIT = 'exit';

/**
 * Request to close the minecraft
 */
export const MINECRAFT_EXIT = 'minecraft-exit';

/**
 * Request start the minecraft
 */
export const MINECRAFT_START = 'minecraft-start';
