{
    "type": "object",
    "properties": {
        "java": {
            "description": "The instance assigned java path on disk.",
            "default": "",
            "type": "string"
        },
        "deployed": {
            "default": "[]\nThe resources already deployed",
            "type": "array",
            "items": {
                "$ref": "#/definitions/DeployedInfo"
            }
        }
    },
    "required": [
        "deployed",
        "java"
    ],
    "definitions": {
        "DeployedInfo": {
            "type": "object",
            "properties": {
                "src": {
                    "description": "If this is deployed by link, it will be a file path to the source.",
                    "type": "string"
                },
                "file": {
                    "description": "Deployed file relative path to the .minecraft folder",
                    "type": "string"
                },
                "url": {
                    "type": "string"
                },
                "resolved": {
                    "description": "The way to resolve it. If it's false, it doesn't resolved.",
                    "enum": [
                        false,
                        "link",
                        "unpack"
                    ]
                }
            },
            "required": [
                "file",
                "resolved",
                "url"
            ]
        }
    },
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false
}